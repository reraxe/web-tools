# Patch Notes — v2.4-live

`v2.4-live` promotes the accepted v2.4 TEST functionality to the first operational DEX LIVE release. It adds no recognition, economics, inventory, receipt, marketplace, JANA, reservation, or DINO behavior.

Visible release changes:

- runtime and health identity are `v2.4-live`;
- the sidebar includes a compact `LIVE` indicator;
- release documentation establishes separate TEST/LIVE writable storage, one-time clean Day Zero, permanent LIVE data lineage, and data-preserving rollback/upgrade rules.

SAM remains suggestion-only until operator confirmation/correction. Exact printing remains operator-only. Migrations remain 0001–0019.
