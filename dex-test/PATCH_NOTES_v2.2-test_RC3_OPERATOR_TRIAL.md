# Patch Notes — v2.2-test RC3 Operator Trial

RC3 is a consolidated release candidate, not a new feature phase. It packages the accepted v2.2-test application, tests, migrations, frontend assets, operating documentation, and isolated shadow research needed for operator trial review.

No application logic, database schema, SAM thresholds, authority rules, economics calculations, UI workflow, Compose/Jenkins behavior, production tag, port, volume, or container name changed during RC3 packaging.

The runtime continues to identify as `v2.2-test`. Human-facing release documentation identifies this checkpoint as **RC3 — Operator Trial**.

External reference images and all physical-validation artifacts are intentionally excluded. Production status is **NOT APPROVED**.

